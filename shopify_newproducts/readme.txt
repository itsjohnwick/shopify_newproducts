Very rudimentary new products monitor on shopify, one of my first projects.

Add proxies (one per line) in proxies.txt, with the format user:pass@ip:port **AT LEAST 1 PROXY REQUIRED**

Add webhook information in webhook.json

Add shopify websites that you would like to monitor in websites.txt, one per line